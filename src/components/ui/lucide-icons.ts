
// This file is intentionally left blank.
// It is used to satisfy the type-checker for lucide-react icons.
// When you use an icon from lucide-react, you can import it like this:
// import { Move } from "lucide-react"
// If the icon is not available, you can add it to this file to satisfy the type checker.

// Example:
// export const Move = () => null;
export const Star = () => null;
